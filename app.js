

//Solo NODE
/*const { read } = require('fs');
const http = require('http');
const hostmname = '127.0.0.1';
const port = 8000;

const serve = http.createServer((req, res) => {
    res.statusCode = 200;
    res.setHeader('Content-type', 'text/plain');
    res.end('Hola Mundo');
});

serve.listen(port, hostmname, () => {
    console.log(`Servidor corriendo en http://${hostmname}:${port}`);
});
*/